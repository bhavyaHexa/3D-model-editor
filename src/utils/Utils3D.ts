import * as THREE from 'three';

export const Utils3D = {
  loadEnvironmentTexture: async (file: File): Promise<THREE.Texture> => {
    // Stub implementation to prevent breaking
    return new THREE.Texture();
  }
};
